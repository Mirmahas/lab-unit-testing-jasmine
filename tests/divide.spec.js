// UNCOMMENT THE CODE BELOW TO START

describe("funcion divide", () => {
  if (
    ("Should be defind",
    () => {
      expect(divide).toBeDefined();
    })
  );
  describe("Function - divide", () => {
    // Each `it` block represents 1 test. You can use the following as a template:
    it("", () => {});
  });
});
